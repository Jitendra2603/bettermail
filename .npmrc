# Enable npm cache
cache=true
prefer-offline=true

# Use frozen lockfile for faster installs
use-frozen-lockfile=true

# Reduce npm logs to speed up installation
loglevel=error

# Disable audit to speed up installation
audit=false
fund=false

# Increase network timeout for better reliability
network-timeout=150000

# Specify npm version
engine-strict=false